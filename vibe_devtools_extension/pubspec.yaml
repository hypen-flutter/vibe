name: vibe_devtools_extension
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  devtools_app_shared: ^0.2.2
  devtools_extensions: ^0.2.2
  devtools_shared: ^10.0.1
  flutter:
    sdk: flutter
  vm_service: ^14.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
