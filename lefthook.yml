pre-commit:
  piped: true
  commands:
    lint:
      glob: "*.dart"
      run: dart fix --apply {staged_files}
    format:
      glob: "*.dart"
      run: dart format {staged_files}
    restage:
      run: git add {staged_files}
